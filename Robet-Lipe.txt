https://codepen.io/RobertDLipe/pen/GRNNBBd?editors=1100
<!--1. fork this codepen so that you have a copy on your account -->
<!--2. Add HTML and CSS to make the codepen look like the example: https://codepen.io/BritHemming/full/jONmxOm  - note that you can change the colours but everything else must look the same -->
<!-- 3. include the link to this completed codepen at the top of today's file first-lastname.txt --> 
<!-- 4. when you are finished - answer the questions at the bottom of this file -->


    1. What is Semantic HTML? 
            This is descriptive html elements. these are used for on-screen readers and seo
    2. What is HTML used for? 
            It is used to for structure of a webpage
    3. What is an attribute and where do we put it? 
            They are additional information in elements, they go in the opening <> for a tag after the element name
    4. What is the h1 tag used for? How many times should I use it on a page?
            It is the most important headed and used only 1 time
    5. Name two tags that have required attributes
            <img> <a>
    6. What do we put in the head of our HTML document? 
            CSS Link,
    7. What is an id? 
            It is a name used to specify a unqiue element in HTML   
    8. What elements can I add an id to? 
            All of them    
    9. How many times can I use the same id on a page? 
            1 time
    10. What is a class? 
            An identifier to name multiple elements 
    11. What elements can I add a class to? 
            Any HTML Element
    12. How many times can I use the same class on a page? 
            Multiple times
    13. How do I get my link to open in a new tab?
            target="_blank" in the <a>    
    14. What is the alt attribute in the image tag used for? 
            This can display a tooltip with a description of the image    
    15. How do I reference an id?
            #idname
    16. What is the difference between a section and a div
            A section is group content with a meaning while a div is just a container for data    
    17. What is CSS used for? 
            Styling html elements
    18. How to we select an element? Example - every h2 on the page
            h2 {}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
            Class is used to style one or many elements and ID is used to style one specific element. class could be used to alter text/background of several <p> tags and id could be used to effect a particular header or <p> that needs to be different
    20. How do we select classes in CSS?
            .classname
    21. How do we select a p element with a single class of “human””?
            <p class="human">
    22. What is a parent child selector? When would this be useful? 
            This is a java script query that can extract tags within tags to make changes to them. this could be used to highlight data within a specific tag <span> within broader tags <p>. 
    23. How do you select all links within a div with the class of sidebar?
            .sidebar a {}    
    24. What is a pseudo selector?
            it is used to define a special state of an element, ex. hover state, visited    
    25. What do we use the change the spacing between lines?
            line-height    
    26. What do we use to change the spacing between letters?
            element {letter-spacing: 30px;}
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
            text-transform    
    28. How do I add a 1px border around my div that is dotted and black?
            div {border: 1px dotted black}
    29. How do I select everything on the page? 
            Ctrl+A
    30. How do I write a comment in CSS?
            /* comment */
    31. How do I find out what file I am in, when I am using the command line? 
            pwd
    32. Using the command line - how do I see a list of files/folders in my current folder?
            ls -al
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
            rm command, this will delete without confirmation
    34. Why should I use version control? 
            It prevents having to keep seperate files for other verison. it keeps all the data in the verions history for easy access.    
    35. How often should I commit to github?
            Often, no reason to keep old data    
    36. What is the command we would use to push our repo up to github? 
            $ git push -u origin Firstname-Lastname
    37. Walk me through Lambda's git flow. 
            we start by making a folder for the project
            we then make a git repo on the local machine using git information
            then we make/locate the online repo and fork the project
            we can clone the fork into the local repo using git clone url
            we can open the clone in VS code to work with it
            when we have made changes we use the git add command to save them
            then git commit -m "with the comment about the change"
            then git push -u origin originname
            this will send it back to github to finish with a pull request to confirm any updates or changes

Stretch Questions

    1. What is the difference between an inline element and a block element?
        Inline aligns the elements horizontally and block aligns then vertically on new lines
    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 

git add.
git commit -m ""
git push -u origin Robert-Lipe

   --> 